# Printful Configuration
# VITE_ variables are for client-side (development)
# VITE_PRINTFUL_TOKEN=your_printful_token_here

# Server-side environment variables (add these in Netlify Dashboard)
# PRINTFUL_TOKEN=your_printful_token_here

# Netlify will automatically use these environment variables during build
# Add PRINTFUL_TOKEN in Netlify Dashboard: Site settings → Build & deploy → Environment
