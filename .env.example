# Printify Configuration
# VITE_ variables are for client-side (non-sensitive data only)
VITE_PRINTIFY_SHOP_ID=your_shop_id_here

# Server-side environment variables (add these in Netlify Dashboard)
# PRINTIFY_TOKEN=your_printify_token_here

# Netlify will automatically use these environment variables during build
# Add PRINTIFY_TOKEN in Netlify Dashboard: Site settings → Build & deploy → Environment
